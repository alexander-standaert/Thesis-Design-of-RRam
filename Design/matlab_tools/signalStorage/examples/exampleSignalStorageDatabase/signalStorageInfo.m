function [] = signalStorageInfo()
    
    fprintf( 'This folder contains an example signalStorageDatabase.             \n\n' );
    fprintf( '(This example is the output of the spectreNonCompressed example)   \n\n' );
    fprintf( 'Run "makePlot" to visualize the result.           \n' );
    fprintf( 'Run "UWIbrowser" to start the browser with those results loaded.     \n' );
    fprintf( 'Run "signalStorageBrowser([path])" to start the browser.           \n\n' );
    
    fprintf( 'You can also run "signalStorageBrowser( sim )" to start the        \n'   );
    fprintf( 'browser using an already loaded simulation database.               \n'   );
    
    
    

end